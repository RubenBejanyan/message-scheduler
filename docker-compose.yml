services:
  message_scheduler:
    image: ghcr.io/rubenbejanyan/message-scheduler:latest
    container_name: message_scheduler
    restart: always
    env_file:
      - .env
    networks:
      - infra_net

networks:
  infra_net:
    external: true
    name: infra_net
